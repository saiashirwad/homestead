# Plan: make the default intervalSeconds 10 seconds (#4)

- [ ] Change `DEFAULT_INTERVAL_SECONDS` in `src/listen.ts` from 30 to 10, and update the stale "default 30" comment on `intervalSeconds` in `src/types.ts` to "default 10"
